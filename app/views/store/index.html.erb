<div class="container">
  <% @items.each do |item| %>
    <%= link_to item.title, item_path(item) %> | <strong><%= number_to_currency(to_currency(item.price)) %></strong><span><%= button_to 'Add to Cart', line_items_path(item_id: item), class: "btn btn-primary" %></span>
  <% end %>
</div>